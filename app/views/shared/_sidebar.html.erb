<div class="d-flex flex-column custom-sidebar">
    <%= image_tag "unite.jpeg" %>
  <div class="d-flex flex-grow-1">
    <div class="d-flex flex-column " style="width: 280px;">
      <p class="fs-4 mx-4">User Name</p>
        <%# <span class="user-name"><%= current_user.email  %>
        <ul class="list-unstyled">
          <li class="nav-item">
            <a href="#" class="nav-link link-dark mb-3" aria-current="page">
              <svg class="bi me-2 my-6"  width="20" height="20"><use xlink:href="#home"></use></svg>
              <% link_to "home", root_path %> Home
            </a>
          </li>
          <li>
            <a href="#bootstrap" class="nav-link link-dark mb-3">
              <svg class="bi me-2 " width="20" height="20"><use xlink:href="#table"></use></svg>
              Events
              <% link_to "home", root_path %>
           </a>
           </li>
           <li>
            <a href="#bootstrap" class="nav-link link-dark mb-3">
              <svg class="bi me-2 " width="20" height="20"><use xlink:href="#grid"></use></svg>
              Messages
            </a>
           </li>
        </ul>
        <% if user_signed_in? %>
          <%= link_to "Log out", destroy_user_session_path, data: {turbo_method: :delete} %>
        <% else %>
          <%= link_to "Login", new_user_session_path %>
        <%end %>
    </div>
  </div>
</div>
