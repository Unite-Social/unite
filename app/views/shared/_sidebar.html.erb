<div class="sidebar" data-sidebar-target='toggle'>
  <button class="sidebar__toggle" type="button" data-action="click->sidebar#open" aria-label="Toggle sidebar"></button>

  <%= image_tag "unite.jpeg" %>
  <div class="d-flex flex-grow-1">
    <div class="d-flex flex-column " style="width: 280px;">
      <p class="fs-4 mx-4"> <% if user_signed_in? %>
  <%= current_user.email %>
  <% else %>
  <h1></h1>
  <% end %></p>
          <%# <span class="user-name"><%= current_user.email  %>
      <ul class="list-unstyled">
        <div class="nav-item mb-2">
          <i class="fa-solid fa-house"></i> <%= link_to "Home", root_path %>
        </div>
        <div class="nav-item mb-2">
          <i class="fa-solid fa-calendar-days"></i> <%= link_to "Messages", chatroom_path(Chatroom.last) %>
        </div>
        <div class="nav-item mb-2">
          <i class="fa-solid fa-address-card"></i> <%= link_to "About", about_path %>
        </div>
        <div class="nav-item mb-2">
          <i class="fa-solid fa-group-arrows-rotate"></i> <%= link_to "Movement", movements_path %>
        </div>
      </ul>
      <div class="mt-auto p-2 bd-highlight">
      <% if user_signed_in? %>
        <%= link_to "Log out", destroy_user_session_path, data: {turbo_method: :delete} %>
      <% else %>
        <button type="button" class="btn btn-outline-dark btn-sm " onclick="window.location.href='<%= new_user_session_path %>'">Login</button>
      <%end %>
      </div>
    </div>
  </div>
</div>
